// settings_window.h — settings dialog window (native controls, dark themed).
#pragma once

namespace cv {

void OpenSettingsWindow();     // creates/activates the settings window
bool IsSettingsWindowOpen();

}  // namespace cv
